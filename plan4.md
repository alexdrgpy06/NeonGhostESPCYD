The memory says: "If a codebase already fails to compile before your modifications, do not attempt to fix the pre-existing build errors if doing so would violate your persona boundaries (e.g., a UX agent modifying backend constants like `HP_CAPTURE` to `HP_BLE_FOUND`). Revert any such unauthorized fixes and submit with the pre-existing errors intact, as it is acceptable to proceed if you did not introduce the failure."
So I will not fix `HP_CAPTURE` or `MP_CAPTURE` since it violates my persona boundaries.

I will now complete the pre-commit steps.
