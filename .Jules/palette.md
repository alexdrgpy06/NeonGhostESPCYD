## 2026-02-24 - Standardized Button Feedback
**Learning:** Centralizing button rendering into a `drawSingleButton` helper simplifies implementation of visual feedback (pressed states) and ensures consistent text alignment across the interface.
**Action:** When implementing new UI elements, always use helper functions to encapsulate visual logic and ensure consistent interaction feedback for users.
